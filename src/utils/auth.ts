// // utils/auth.ts

// export const isAuthenticated = (): boolean => {
//   // Nesta função, você faria a lógica de verificação de autenticação real.
//   // Por exemplo, checando a existência de um token no localStorage ou em um cookie.
//   // Exemplo:
//   // const token = localStorage.getItem('authToken');
//   // return !!token;

//   // Por enquanto, vamos simular que o usuário está logado.
//   // Mude para `false` para testar o redirecionamento.
//   return true;
// };